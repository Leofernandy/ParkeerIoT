<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/white" /> <!-- warna isi card -->
    <stroke
        android:width="1dp"
        android:color="@color/merah" /> <!-- warna garis tepi -->
    <corners android:radius="6dp" />
</shape>